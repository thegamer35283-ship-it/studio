
{
  "entities": {
    "Donor": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Donor",
      "type": "object",
      "description": "Represents an individual or organization supporting the charity.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Donor entity."
        },
        "firstName": {
          "type": "string",
          "description": "The first name of the donor."
        },
        "lastName": {
          "type": "string",
          "description": "The last name of the donor."
        },
        "email": {
          "type": "string",
          "description": "The email address of the donor.",
          "format": "email"
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the donor record was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp when the donor record was last updated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "firstName",
        "lastName",
        "email",
        "createdAt"
      ]
    },
    "Campaign": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Campaign",
      "type": "object",
      "description": "Represents a specific charitable cause or project.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Campaign entity."
        },
        "name": {
          "type": "string",
          "description": "The name of the charitable campaign."
        },
        "description": {
          "type": "string",
          "description": "A detailed description of the campaign's goals and impact."
        },
        "isActive": {
          "type": "boolean",
          "description": "Indicates if the campaign is currently active."
        },
        "category": {
          "type": "string",
          "description": "The category the campaign belongs to."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the campaign was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp when the campaign was last updated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "name",
        "description",
        "isActive",
        "category",
        "createdAt"
      ]
    },
    "StaffMember": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "StaffMember",
      "type": "object",
      "description": "Represents a member of the organization's staff or dedicated volunteers.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the StaffMember entity."
        },
        "name": {
          "type": "string",
          "description": "The full name of the staff member."
        },
        "email": {
          "type": "string",
          "description": "The email address of the staff member.",
          "format": "email"
        },
        "role": {
          "type": "string",
          "description": "The job title or role (e.g., 'Field Coordinator', 'Accountant')."
        },
        "status": {
          "type": "string",
          "description": "Employment status.",
          "enum": ["Active", "Inactive"]
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the record was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp when the record was last updated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "name",
        "role",
        "status",
        "createdAt"
      ]
    },
    "BeneficiaryCommunity": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "BeneficiaryCommunity",
      "type": "object",
      "description": "Represents a specific group or region that benefits from charitable activities.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the BeneficiaryCommunity entity."
        },
        "name": {
          "type": "string",
          "description": "The name of the beneficiary community."
        },
        "description": {
          "type": "string",
          "description": "A description of the community and its needs."
        },
        "geographicRegion": {
          "type": "string",
          "description": "The geographical area where this community is located."
        },
        "type": {
          "type": "string",
          "description": "The type or demographic of the community."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the beneficiary community record was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp when the beneficiary community record was last updated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "name",
        "description",
        "geographicRegion",
        "type",
        "createdAt"
      ]
    }
  },
  "auth": {
    "providers": [
      "password"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/donors/{donorId}",
        "definition": {
          "entityName": "Donor",
          "schema": {
            "$ref": "#/backend/entities/Donor"
          },
          "description": "Stores individual donor profiles."
        }
      },
      {
        "path": "/campaigns/{campaignId}",
        "definition": {
          "entityName": "Campaign",
          "schema": {
            "$ref": "#/backend/entities/Campaign"
          },
          "description": "Main collection for charitable campaigns. Publicly readable."
        }
      },
      {
        "path": "/staff/{staffId}",
        "definition": {
          "entityName": "StaffMember",
          "schema": {
            "$ref": "#/backend/entities/StaffMember"
          },
          "description": "Collection of staff members and volunteers."
        }
      },
      {
        "path": "/beneficiary_communities/{communityId}",
        "definition": {
          "entityName": "BeneficiaryCommunity",
          "schema": {
            "$ref": "#/backend/entities/BeneficiaryCommunity"
          },
          "description": "Contains details about the communities or groups that benefit from campaigns."
        }
      },
      {
        "path": "/admins/{uid}",
        "definition": {
          "entityName": "AdminRole",
          "schema": {
            "type": "object",
            "properties": {
              "uid": { "type": "string" }
            }
          },
          "description": "Implements DBAC for administrators. Existence of a document grants privileges."
        }
      }
    ],
    "reasoning": "Removed all donation and payment related entities and paths as requested."
  }
}
